---
interface Props {
  collapseTitle: String;
  customCss: String;
}
const { collapseTitle, customCss } = Astro.props;
---

<details>
  <summary class = `font-serif text-header${customCss}`>{collapseTitle}</summary>
  <slot/>
</details>
